<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XamForms.Shell.Bdd.Views.LoginPage">

    <StackLayout Spacing="40" VerticalOptions="CenterAndExpand">
        <!-- Place new controls here -->
        <Label Text="{Binding Error}" TextColor="Red"/>
        <Entry Placeholder="Username" Text="{Binding Username}" WidthRequest="100"/>
        <Entry Placeholder="Password" Text="{Binding Password}" WidthRequest="100"/>
        <Button Text="Login" Command="{Binding LoginCommand}" />
    </StackLayout>

</ContentPage>
